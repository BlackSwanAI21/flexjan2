export interface Agent {
  id: string;
  user_id: string;
  assistant_id: string;
  name: string;
  model: string;
  instructions: string;
  first_message: string;
  created_at: string;
  updated_at: string;
}